# @reown/appkit-adapter-wagmi pulls a transitive optional peer (ox, via
# permissionless) that conflicts with the ox viem ships. The peer is optional
# and the package is never installed, so the conflict is cosmetic -- but npm
# refuses the install without this. Keep it so CI resolves the same tree.
legacy-peer-deps=true
